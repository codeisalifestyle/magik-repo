# AcmeAuth

Internal auth service for Acme Corp's product suite.

## What's here

```
.
├── README.md                     ← this file
├── docs/                         ← project docs (markdown)
│   └── auth-policy.md
└── codebase/
    └── src/
        ├── auth.ts
        └── middleware.ts
```

The project's documentation lives in `docs/`. Source code lives in `codebase/src/`.
